{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "docs.schema.json",
  "title": "LibX Docs Registry Schema",
  "description": "新ドキュメントジェネレーター用のレジストリ定義。CLI・ビルド・移行ツールの単一ソースを提供する。",
  "type": "object",
  "required": [
    "$schemaVersion",
    "projects",
    "settings"
  ],
  "additionalProperties": false,
  "properties": {
    "$schemaVersion": {
      "description": "スキーマの SemVer。互換性判定とマイグレーション判定に利用する。",
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$",
      "examples": [
        "1.0.0",
        "1.1.0"
      ]
    },
    "metadata": {
      "description": "レジストリ生成日時や担当者などの運用メタデータ。キーは任意。",
      "type": "object",
      "additionalProperties": true
    },
    "projects": {
      "description": "管理対象プロジェクトのリスト。`id` は一意である必要がある。",
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "./schema/project.schema.json"
      }
    },
    "settings": {
      "description": "レジストリ全体に適用される設定値。",
      "$ref": "./schema/settings.schema.json"
    }
  }
}
